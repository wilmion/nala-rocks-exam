import { createTheme } from "@mui/material";

export const themeMui = createTheme({
  typography: {
    fontFamily: "Nunito, Arial",
  },
  palette: {
    primary: { main: "#0a58ca" },
    secondary: { main: "#1e2432" },
  },
});
